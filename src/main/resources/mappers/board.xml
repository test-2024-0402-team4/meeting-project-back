<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meeteam4.meeting.repository.BoardMapper">
    <insert id="saveBoard" parameterType="com.meeteam4.meeting.entity.Board">
        insert into student_board_tb
        values(
            0,
            #{studentId},
            #{title},
            #{content},
            #{theme},
            #{viewCount},
            now(),
            now()
        )
    </insert>

    <insert id="saveComment" parameterType="com.meeteam4.meeting.entity.Comment">
        insert into student_comment_tb
        values(
        0,
        #{studentBoardId},
        #{studentUserId},
        #{comment},
        now(),
        now()
        )

    </insert>
</mapper>